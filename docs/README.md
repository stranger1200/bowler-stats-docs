# GitHub Pages output (optional)

If your repository uses the **`/docs`** folder as the GitHub Pages source, run **`npm run build`** at the repo root, then copy everything from **`dist/`** into this folder (overwriting as needed) and commit.

The app source lives at the repository root (`src/`, `public/`).
